# Hero Game

The hero-game system and its active Pixel Fighter implementation are documented in [`pixel-fighter.md`](./pixel-fighter.md).

This compatibility pointer is retained so existing references to `docs/features/hero-game.md` continue to resolve. Update the canonical Pixel Fighter document instead of adding content here.
